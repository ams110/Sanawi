# سياق ثابت لمن يعمل على هذا المستودع

**سنوي تطبيق خاص، ليس منتجاً للعامة.** يخدم صاحبه ودائرته الصغيرة
(~14 مستخدماً من الأهل والمعارف)، ولا يستهدف سوقاً ولا ينافس أحداً.

يترتب على ذلك عند تقييم أي ميزة أو قرار:

- المعيار ليس «هل يشتريها السوق؟» بل «هل يستعملها صاحبها فعلاً؟»
- لا وزن لحجج المنافسة أو النمو أو اكتساب مستخدمين.
- الوزن كله لبساطة الاستعمال اليومي واستمرار صاحبه عليه —
  فالتعقيد الذي يتعبه هو الخطر الأول، إذ لا فريق دعم ولا مستخدمون بدلاء.

---

# قواعد الحسابات — مستخلصة من تدقيق آب 2026

تدقيق شامل ([`docs/audit-2026-08.md`](docs/audit-2026-08.md)) وجد ~40 عطلاً
والاختبارات الـ490 كلها خضراء. كل قاعدة هنا وُلدت من عطل حقيقي وقع —
**تُقرأ قبل أي ميزة أو تعديل يمسّ رقماً**، وخطة الإصلاح في
[`docs/fix-plan.md`](docs/fix-plan.md).

## 1. سؤال واحد = محرّك واحد

كل سؤال رقمي («كم الباقي؟»، «كم المبلغ المقترح؟»، «كم حصّتي؟») تجيبه دالة
واحدة في `src/lib/`، والشاشات وMCP يستهلكانها. ممنوع حساب مالي مضمَّن في
مكوّن أو في `mcp/` — ولا «نسخة مطابقة مؤقتاً»: النسختان المتطابقتان اليوم
تنحرفان صامتتين غداً (وقع ذلك: ثلاث صيغ لـ«المبلغ المقترح»، وقاعدتان
لتاريخ دفع الفاتورة، وثلاث قواعد لتحقّق الحصص).

## 2. عالَم الرقم جزء من تعريفه

كل رقم مال إمّا من **عالم الخطة** (المتوقَّع) أو **عالم الواقع** (الواصل
والمصروف فعلاً). ممنوع: خلط العالمين في معادلة واحدة (دخل نصف شهر ناقص
التزامات شهر كامل)، أو عرض رقمين من عالمين متجاورين بلا تصريح، أو شرط
يقلب الأساس ضمنياً (`receivedIncome > 0`). البطاقة تصرّح بعالمها.

## 3. الحصّة تُحسم عند المنبع

«حصّتي أم المبلغ الكامل؟» يُقرَّر مرة واحدة في طبقة تجهيز المدخلات، وكل
محرّك يستقبل أساساً واحداً معلَناً. الحصة تُقَصّ 0–100 بدالة مشتركة واحدة.
وإيداع الشريك **ليس إيداعي** — استثناء صفوف `partnerId` قاعدة واحدة مشتركة
لا قاعدتان «متشابهتان».

## 4. القوائم تصدق والعدّادات تعدّ الكل

عنوان القائمة يطابق محتواها دلالياً (دخلٌ جايك ليس «ضلّ عليك»)، والعدّاد
يشمل المخفي (`items.length + hiddenCount`)، والتحذير يسمّي **السبب المهيمن
فعلاً** في الرقم لا أول سبب خطر ببال الصياغة («بوتيرة صرفك» والصرف 3% من
العجز — كذبة).

## 5. لا رقمَ يَعِد بمالٍ لم يصل

كانت هذه القاعدة «أسبوعي = ‏52/12 من `FREQUENCY_TO_MONTHLY` حصراً»، ثم
**أُلغي الدخل المتوقَّع كلّه** (قرار 16/08/2026،
[`docs/income-actual-plan.md`](docs/income-actual-plan.md)): ‏52/12 يصف
سنةً لا شهراً، فالشهر إمّا أربع قبضات أو خمس، والميزانية منتفخة في كل
شهرٍ رباعيّ بانتظامٍ لا صدفة.

فالقاعدة الآن أعمّ: **أرقام الشهر من `income_entries` وحدها — ما وصل
فعلاً.** ممنوع أن يُشتقّ رقمٌ مالي من تقديرٍ يكتبه المستخدم بيده ثم
يُضرب في معامل. ومصادر الدخل أسماءٌ للتصنيف: `amount` و`frequency` و
`is_variable` أعمدةٌ موروثة لا يقرؤها محرّك (هجرة 0022).

والدرس الباقي من الصيغة القديمة: رقمٌ يبقى معروضاً بعد أن كفّ عن دخول أيّ
حسبة أسوأ من رقمٍ خاطئ — صاحبه يصدّقه ويبني عليه، ولا شيء يقول له إنه لم
يعد يعني شيئاً.

## 6. المدخل الفاسد يُحرس عند بوّابة المحرّك

`finite` و`clamp` على كل مدخلات المحرّكات: مبلغ سالب، حصة فوق 100، NaN من
الملف الشخصي — كلها وقعت. تعليقُ `budget/calc.ts` عند `finite` يشرح لماذا
`Math.max(0, NaN)` وهمُ بوّابة.

## 7. الزمن يُختبر بساعةٍ غير منتصف الليل

كل اختبار يمرّر `today` يضيف حالة بساعة نهارية. الحالات الإلزامية: يوم
الاستحقاق نفسه (ليس «فائتاً»)، آخر يوم في الشهر، يوم 30/31 في شباط،
انتقال كانون أول ← ثاني. مقارنة التواريخ بمفاتيح يوم (`toDateKey`) لا
بطوابع زمنية مختلطة الدقة.

## 8. اختبار المحرّك وحده لا يكفي — التناسق يُفحص صراحةً

490 اختباراً أخضر والشاشة تناقض نفسها. عند إضافة رقم جديد يُسأل دوماً:
أي بطاقة أخرى وأي أداة MCP تعرض مشتقّاً من نفس البيانات؟ وأضف تأكيداً
أنهما من نفس النداء — `npm run check:parity` يفتح الشاشة في متصفح ويسأل
الخادم عبر MCP على نفس القاعدة المزيّفة ويقارن حرفياً، وهو البيت الطبيعي
لهذه التأكيدات.

## 9. القرار المؤجَّل يُسجَّل ويُحذَّر منه في الواجهة

دَينٌ تقني يمسّ أرقام المستخدم (عدّ مزدوج حسابات/أصول، تفسيران للعائد)
لا يكفي توثيقه للمطوّر في README — المستخدم الذي تكذب عليه أرقامه يستحق
تحذيراً على الشاشة حتى يُتّخذ القرار.

## قائمة ما قبل الدمج لأي ميزة تمسّ رقماً

- [ ] المحرّك في `src/lib/` والشاشة وMCP يستهلكانه (قاعدة 1)
- [ ] عالم كل رقم معلَن: خطة أم واقع (قاعدة 2)
- [ ] أساس الحصة محسوم عند المنبع (قاعدة 3)
- [ ] بوّابة مدخلات فاسدة + اختباراتها (قاعدة 6)
- [ ] اختبارات بساعة نهارية وحواف شهور (قاعدة 7)
- [ ] تأكيد تناسق مع كل سطح يعرض مشتقّ نفس البيانات (قاعدة 8)
