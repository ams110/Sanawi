# سنوي

تطبيق يحوّل الالتزامات السنوية الكبيرة إلى أقساط شهرية يدفعها المستخدم لنفسه،
فلا تفاجئه الدفعة عند موعدها.

المشكلة: العقل يحسب المصروف السنوي على أنه "ليس شهرياً"، فتبدو الميزانية مريحة
حتى تصل دفعة كبيرة دفعةً واحدة. الحل: كل التزام ÷ الشهور المتبقية لموعده = قسط
شهري يذهب إلى صندوق مخصص لذلك البند.

## التشغيل

```bash
npm install
npm run dev
```

## الأوامر

| الأمر | ماذا يفعل |
|---|---|
| `npm run dev` | خادم التطوير على المنفذ 5173 |
| `npm run build` | فحص الأنواع ثم بناء الإنتاج |
| `npx vitest run` | اختبارات محرّك الحسابات |
| `node scripts/screenshot.mjs` | لقطات الوضعين الفاتح والغامق + فحص وضع الجسر (يتطلب `npm run dev` يعمل) |

## البنية

```
src/lib/obligations/calc.ts   محرّك الحسابات — نقي، بلا React ولا Supabase
src/lib/format.ts             تنسيق العملة والتواريخ (العملة واللغة وسيطات لا ثوابت)
src/lib/theme.tsx             الثيم: النظام / فاتح / غامق
src/components/ui/            مكوّنات الواجهة
```

`calc.ts` هو قلب التطبيق: كل تغيير في المنطق يبدأ منه ومن اختباراته في
`calc.test.ts`، لا من الواجهة.

## الحالة

المرحلة 1 قيد التنفيذ — محرّك الحسابات ونظام التصميم جاهزان. جداول Supabase
والمصادقة وشاشة الالتزامات الحقيقية لم تُبنَ بعد.
